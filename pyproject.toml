[tool.poetry]
name = "employee-bot"
version = "0.1.0"
description = "Employee management bot built with Python and Flask."
authors = ["Mohammad Qazzaz <your_email@example.com>"]
license = "MIT"
readme = "README.md"

# 🛠️ هذا السطر يحل المشكلة: يخبر Poetry بتثبيت مجلد الكود المصدري الخاص بك
packages = [
    {include = "employee_management_bot"} 
]

[tool.poetry.dependencies]
python = "^3.13" # يستخدم إصدار بايثون 3.13 المحدد في سجلاتك
flask = "^3.1.2"
psycopg2-binary = "^2.9.11"
apscheduler = "^3.11.1" 

# يتم تحديد الإضافات (extras) مثل [job-queue] بهذه الصيغة
python-telegram-bot = {version = "^22.5", extras = ["job-queue"]} 


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"

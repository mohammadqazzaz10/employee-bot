# مكتبة تيليجرام (الإصدار المحدد 13.7 كما طلبت)
python-telegram-bot==13.7

# مكتبة الاتصال بقاعدة بيانات PostgreSQL
# نستخدم -binary لضمان التوافق مع بيئة Render
psycopg2-binary==2.9.1

# لقراءة متغيرات البيئة محلياً (مثل التوكن)
python-dotenv==1.0.0
